html
   head
      title Login
   body
      if(message)
         h4 #{message}
      form(action = "/login" method = "POST")
         input(name = "user_id" type = "text" required placeholder = "User ID")
         input(name = "password" type = "password" required placeholder = "Password")
         button(type = "Submit") Login me!